eureka:
  instance:
    hostname: localhost
  client:
    registerWithEureka: false
    fetchRegistry: false
    service-url:
      default-zone: https://${eureka.instance.hostname}:${server.port}/eureka/
server:
  port: 8761